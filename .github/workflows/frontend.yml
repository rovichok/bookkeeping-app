name: Frontend Build Check

on:
  push:
    paths:
      - "lentis-admin/**"
      - ".github/workflows/frontend.yml"
  pull_request:
    paths:
      - "lentis-admin/**"

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: lentis-admin

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build frontend
        run: npm run build
